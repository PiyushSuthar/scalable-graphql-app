# Types
type Book implements Timestamps {
    id: ID!
    name: String!
    author: Author!

    # Interface Required
    createdAt: Date!
    updatedAt: Date!
}

# Inputs
input BookCreateInput {
    name: String!
    author: ID!
}

input BookUpdateInput {
    name: String!
}